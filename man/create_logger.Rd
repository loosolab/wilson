% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/global.R
\name{create_logger}
\alias{create_logger}
\title{create a logger used within the package}
\usage{
create_logger(logfile = NULL, level = "INFO", name = "wilson")
}
\arguments{
\item{logfile}{Path to a file log messages should be written to. NULL (default) logs to the console only.}

\item{level}{Threshold level of the logger. Messages below this level are dropped. One of "TRACE", "DEBUG", "INFO", "WARN", "ERROR", "FATAL".}

\item{name}{Name of the logger. \code{lgr} caches loggers by name, so distinct names (e.g. a Shiny session token) yield independent loggers.}
}
\value{
An \code{lgr} \code{\link[lgr]{Logger}} object.
}
\description{
create a logger used within the package
}
\details{
Thin convenience wrapper around \code{\link[lgr]{get_logger}} that sets the threshold and, optionally, attaches a file appender (\code{\link[lgr]{AppenderFile}}). The returned object can be handed to \code{\link{set_logger}}.
}
\examples{
logger <- create_logger(level = "INFO")
set_logger(logger, token = "example")

}
